# Machine Learning Module

This module handles:
- Data preprocessing
- Model training for food recommendations
- Prediction services

## Usage
1. Install dependencies: `pip install -r requirements.txt`
2. Preprocess data: `python scripts/preprocess.py`
3. Train model: `python scripts/train.py`